#include <bits/stdc++.h>

#define ll long long int

using namespace std;

int main() {
    ll t;
    cin >> t;
    while (t > 0) {
        ll n;
        cin >> n;
        vector<ll> a(n);
        for (ll i = 0 ; i < n ; i++) {
            cin >> a[i];
        }
        t--;
    }
    return 0;
}